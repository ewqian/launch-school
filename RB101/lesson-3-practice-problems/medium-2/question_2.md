a, b & c have the same object_id.

This happens because numbers are immutable aka their value can't change. So every variable that references the number 42 will reference the same immutable integer object.